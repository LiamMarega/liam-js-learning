---

---

<script>
  // Only redirect if we're actually on the /paint page
  if (typeof window !== "undefined" && window.location.pathname === "/paint") {
    sessionStorage.setItem("openWindow", "/paint");
    window.location.href = "/";
  }
  if (
    typeof window !== "undefined" &&
    window.location.pathname === "/chatbot"
  ) {
    sessionStorage.setItem("openWindow", "/chatbot");
    window.location.href = "/";
  }
</script>

<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="refresh" content="0; url=/" />
    <title>Página no encontrada</title>
    <style>
      body {
        background-image: url("../../assets/bg/windows-bg.webp");
        background-size: cover;
        background-position: center center;
        background-repeat: no-repeat;
        min-height: 100vh;
        width: 100%;
        height: 100vh;
        color: var(--text);
        font-family: var(--font-family);
        font-size: var(--font-size-base);
        line-height: 1.5;
        margin: 0;
        padding: 0;
        position: relative;
      }
    </style>
  </head>
  <body> </body>
</html>
